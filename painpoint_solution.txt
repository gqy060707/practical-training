痛点：系统缺少两个景点之间的最短路径查询功能。

解决方案：
1. 在 traves.c 中实现 Floyd 最短路径算法（shortPath）。
2. 通过 minDistance 函数读取起点和终点名称，调用最短路径计算结果。
3. 使用 printPath 递归输出从起点到终点的路径序列。
4. 在 main.c 菜单中将选项 5 绑定到 minDistance，用户可直接查询最短距离和路径。

效果：用户创建旅游景点图后，可通过菜单项 5 输入起点和终点，系统返回最短距离与具体路径。